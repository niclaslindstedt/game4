# Conformance with OSS_GAME_SPEC.md

[`OSS_GAME_SPEC.md`](../OSS_GAME_SPEC.md) at the repository root is this project's governing specification, and the committed copy IS the spec — a verbatim copy of the one the sibling games carry, self-contained, amended in a reviewed pull request, with no upstream document to reconcile against and no validator to phone. This page is the other half of that arrangement: **one row per chapter, a verdict, the evidence behind it, and — where the repo falls short — what closing the gap would take.**

It exists because a conformance claim with nothing behind it is a badge. A sweep walks the spec chapter by chapter and re-dates every row; a sweep that changes no code still updates this file, because a verdict with no date behind it is an opinion.

**This repository is a first vertical slice, bootstrapped from the sibling jet-ski game's infrastructure**, and the ledger says so rather than rounding every unbuilt chapter up. A chapter whose subject is planned but not in the tree is **not yet built** — not N/A (the condition WILL apply) and not conformant (nothing is there to conform).

**Verdicts:** ✅ conformant · ◐ partial, with the gap named · ✗ not conformant · ○ not yet built · ⊘ deliberate deviation, with the reasoning · — not applicable, with the reason.

_Last walked: 2026-09-23, against spec v1.1.0 — the bootstrap walk, before the engine and the app had landed; the game-shape rows are the slice's contract rather than a reading of built code, and the next walk re-reads them against the tree._

## The OSS baseline (§1–§22)

| §         | Subject                                              |  V  | Evidence, and the gap                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------- | ---------------------------------------------------- | :-: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1         | Repository layout                                    | ✅  | `engine/`, `pwa/`, `tests/`, `scripts/`, `docs/`, `.github/`, `.githooks/`, `.changes/` at the root, with the spec beside them.                                                                                                                                                                                                                                                                                         |
| 2         | License                                              | ✅  | `LICENSE`: PolyForm Noncommercial 1.0.0 with its `Required Notice:` line naming this repository; SPDX headers on every source file.                                                                                                                                                                                                                                                                                     |
| 3         | README                                               | ✅  | What, why, prerequisites, install, quick start, usage table, controls, configuration and deploy slots, troubleshooting, architecture, docs, contributing, license.                                                                                                                                                                                                                                                      |
| 4–6       | Contributing, CoC, Security                          | ✅  | `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md` (private advisories).                                                                                                                                                                                                                                                                                                                                            |
| 7         | `AGENTS.md` + symlinks                               | ✅  | The five tool aliases are symlinks onto `AGENTS.md` — the router, written against the tree — held by `tests/symlinks_test.ts` and CI's `symlinks` job.                                                                                                                                                                                                                                                                  |
| 7.2       | The four game sections                               | ✅  | `AGENTS.md` carries the role map, what is generated, where new code goes, the single sources ("Stated once"), the parity rules and the skills.                                                                                                                                                                                                                                                                          |
| 8         | Commits, SemVer, CHANGELOG                           | ✅  | Conventional commits enforced by `.githooks/commit-msg`; `CHANGELOG.md` written only by the release, its shape held by `tests/changeset_test.ts`.                                                                                                                                                                                                                                                                       |
| 8.5       | Fragment or `no-changelog`                           | ✅  | `.changes/unreleased/`, `scripts/release/check-changeset.mjs` in CI's `changeset` job, every fragment parsed by `tests/changeset_test.ts`.                                                                                                                                                                                                                                                                              |
| 9         | Makefile entry points                                | ✅  | `build`, `test`, `lint`, `fmt`, `fmt-check`, `hooks`, `icons`, `clean`, `install`, `changelog`, `bump`.                                                                                                                                                                                                                                                                                                                 |
| 9.1       | The game's five targets                              |  ◐  | `sim`, `level`, `analyze`, `ride`, `screenshots`, `profile`, `audition` are registered; the scripts behind them land with the engine and the app, and a target is conformant the day its script answers `--help`.                                                                                                                                                                                                       |
| 10        | CI and release                                       | ✅  | `ci.yml` (tests over two shards, format, lint, build, simulate, shell-lint, symlinks, changeset), `version-bump.yml` → `release.yml` → `pages.yml`, one dispatched run with no PAT.                                                                                                                                                                                                                                     |
| 11.1      | `docs/`                                              |  ◐  | `configuration.md`, `platforms.md` and this page. **Gap:** architecture, getting started, the riding model, the generator and the simulator each owe a page as their subject lands.                                                                                                                                                                                                                                     |
| 11.2      | The web presence                                     |  ⊘  | The deployed site IS the product; there is no separate website tree and no marketing copy. Same decision as the siblings.                                                                                                                                                                                                                                                                                               |
| 11.3      | SEO and discoverability                              |  ⊘  | Deliberately not indexed: `noindex` on every page, `robots.txt` `Disallow: /`, no description, canonical, cards, JSON-LD, sitemap or `llms.txt` — held by `tests/identity_test.ts`. See [configuration.md](configuration.md#discoverability).                                                                                                                                                                           |
| 11.4      | The page is the game                                 |  ◐  | `pwa/index.html` mounts the app and nothing else. **Gap:** the app is a placeholder boot card until the shell lands.                                                                                                                                                                                                                                                                                                    |
| 11.4.8    | Disjoint preview scopes                              | ✅  | Each slot gets its own manifest `id`/`scope`/`start_url` and precache id (`cacheIdForBase`); the root worker disowns `/preview/` and `/branch/`.                                                                                                                                                                                                                                                                        |
| 11.5      | Deployment slots                                     | ✅  | `/`, `/preview/`, `/branch/` in `pages.yml`, at `game4.niclaslindstedt.se`.                                                                                                                                                                                                                                                                                                                                             |
| 12        | The developer toolbelt                               |  ◐  | Every tool parses its flags through `scripts/lib/cli.mjs` (`--help`, non-zero on an unknown flag). **Gap:** the labs themselves are being written.                                                                                                                                                                                                                                                                      |
| 13.1      | Worked examples                                      |  ○  | No `examples/` yet; known-good seeds are recorded once the generator is tuned.                                                                                                                                                                                                                                                                                                                                          |
| 13.2      | `prompts/`                                           |  —  | The game calls no model at runtime or build time.                                                                                                                                                                                                                                                                                                                                                                       |
| 14–18     | Dependencies, templates, hooks, governance, channels | ✅  | `dependabot.yml` (npm + actions), issue and PR templates, `.githooks/`, `CODEOWNERS`, governance and channels in `CONTRIBUTING.md`.                                                                                                                                                                                                                                                                                     |
| 19.4      | Central output module                                | ✅  | `engine/output.ts`; the app wires it through `pwa/src/output-bridge.ts`, and `tests/imports_test.ts` refuses a `console.` anywhere in the engine.                                                                                                                                                                                                                                                                       |
| 19.5      | Diagnostics in a game                                |  ◐  | The DEVELOPER page (the front door's title held seven seconds) is the in-build surface: live state (every probe's load, sink and travel, the frame rate and the frame's cost), the engine's debug switch (`setDebugEnabled`) and the last lines of its ring buffer on screen, and a REPRO line that stages the frame from a URL (`?t=`, `?pose=`). **Gap:** the ring buffer is shown, not yet dumped whole as a report. |
| 20.1–20.4 | Test layout                                          | ✅  | Root `tests/<topic>_test.ts`, vitest, sharded in CI.                                                                                                                                                                                                                                                                                                                                                                    |
| 20.5      | The 1000-line cap                                    | ✅  | `tests/file_size_test.ts`, with the §20.5.1 marker refused when it has no reason or no need.                                                                                                                                                                                                                                                                                                                            |
| 21        | Skills                                               | ✅  | `.agents/skills/` ported from the sibling where the subject exists here, `.claude/skills` and `.gemini/skills` symlinked onto it, each skill with front matter and an empty `.lessons/`, the maintenance skills with `.last-updated`; `tests/skills_test.ts` holds the directory, the router's Skills section and the `maintenance` registry together.                                                                  |
| 22        | Bootstrap checklist                                  | ✅  | Everything above.                                                                                                                                                                                                                                                                                                                                                                                                       |

## The game shape (§23–§41)

| §         | Subject                          |  V  | Evidence, and the gap                                                                                                                                                                                                                                                                                    |
| --------- | -------------------------------- | :-: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 23.1      | The simulation core              |  ◐  | `engine/`, framework-free, one surface at `engine/index.ts` (`@engine`). Being written; the direction is already held.                                                                                                                                                                                   |
| 23.2      | The presentation shell           |  ◐  | `pwa/`, Preact + three.js, reads state and never steps it. The scaffold (build, service worker, identity) is done; the renderer and the HUD are being written.                                                                                                                                           |
| 23.3      | Platform shells                  |  ○  | `tauri/` and `native/` planned; `pwa/src/shell-host.ts` and the release seam are in place ([platforms.md](platforms.md)).                                                                                                                                                                                |
| 23.4      | The session service              |  —  | Single-player; no server.                                                                                                                                                                                                                                                                                |
| 23.5      | Content                          |  ⊘  | Maps are GENERATED from a seed rather than authored, and the one sled and the tuning are TypeScript consts rather than schema-validated data — the siblings' reasoning, carried over: a catalog of one is not worth a loader.                                                                            |
| 23.6      | Tooling                          | ✅  | `scripts/` imports anything; nothing imports it.                                                                                                                                                                                                                                                         |
| 23.7      | Direction enforced by a test     | ✅  | `tests/imports_test.ts` walks the real import graph: the engine imports nothing outside itself and no package, the app reaches it only through `@engine`, nothing imports tooling, and the engine reads no clock and no `Math.random`.                                                                   |
| 23.8      | The sequel test                  |  ○  | Holds once the physics suites stage on a synthetic map rather than a generated one.                                                                                                                                                                                                                      |
| 23.9      | The startup-path budget          |  ✗  | No budget is measured; three.js must stay off the first-render path by a dynamic import, and nothing holds that yet.                                                                                                                                                                                     |
| 24        | Content is data                  |  ⊘  | See §23.5.                                                                                                                                                                                                                                                                                               |
| 25.1      | Seeded, fixed-step runs          |  ◐  | 120 Hz `step(state, input)`, randomness only through `state.rng` — the contract; a determinism test lands with the engine.                                                                                                                                                                               |
| 25.2–25.4 | Dice, digests, authority         |  ○  | The sim's digest and a replay guard land with `engine/sim/`.                                                                                                                                                                                                                                             |
| 26–27     | Scripted rules, mods             |  —  | Neither is in scope.                                                                                                                                                                                                                                                                                     |
| 28        | Interface as content             |  ✗  | The interface will be code, as in the siblings.                                                                                                                                                                                                                                                          |
| 29.1      | Assets from one source           | ✅  | No asset files: every sled, hill and tree is code, every sound synthesized; the icons are rendered from the mark by `make icons`, held to the SVG by `tests/app_mark_test.ts` and to the palette by `tests/identity_test.ts`.                                                                            |
| 29.2      | A shared style definition        |  ◐  | `pwa/src/styles.css` restates `PALETTE`. **Gap:** it is the sibling's stylesheet carried over, still reading the old variable names, until the app's own pass prunes it.                                                                                                                                 |
| 29.3      | Audio is content                 |  ○  | The synth and the beds are being written.                                                                                                                                                                                                                                                                |
| 29.4      | Generated store rasters          |  —  | No store listing.                                                                                                                                                                                                                                                                                        |
| 30        | Narrative                        |  —  | None.                                                                                                                                                                                                                                                                                                    |
| 31        | Naming and IP safety             |  ◐  | Nothing names a real product, brand or manufacturer; the sibling games' names are refused on every player-facing surface by `tests/identity_test.ts`. **Gap:** no test sweeps the whole tree for brands.                                                                                                 |
| 32.1–32.3 | The loop, the simulator, the bot |  ○  | `make sim` and CI's `simulate` job are registered; `engine/sim/` is being written.                                                                                                                                                                                                                       |
| 32.4–32.6 | Playtest, benchmark, staging     |  ◐  | `make screenshots` and `make profile` are registered; DEVELOPER ▸ BENCHMARK (`make bench` headlessly) times a pinned race's frames in the real build, scores it and keeps the last twenty runs. **Gap:** no baseline is committed, and there is no headless steps-per-second benchmark against a digest. |
| 33        | Shells, store metadata           |  ○  | See §23.3; store metadata is out of scope.                                                                                                                                                                                                                                                               |
| 34        | Multiplayer                      |  —  | Single-player (rivals are bots stepped by the same function as the player).                                                                                                                                                                                                                              |
| 35.1      | Reference device and frame rate  |  ✗  | Not named yet.                                                                                                                                                                                                                                                                                           |
| 35.2–35.4 | Viewport, perception, access     |  ○  | The HUD and its viewports arrive with the app.                                                                                                                                                                                                                                                           |
| 35.5      | The mature-content gate          |  —  | No mature content.                                                                                                                                                                                                                                                                                       |
| 35.6      | One identity manifest            | ✅  | `pwa/src/identity.ts`; every restatement (`index.html`, `public/`, the icon generator, both package manifests, the README) held by `tests/identity_test.ts`.                                                                                                                                             |
| 36        | Save data and compatibility      |  —  | Nothing is saved in the slice. The first persisted structure owes a version field and a per-app storage prefix.                                                                                                                                                                                          |
| 37        | Input, presence, time            |  ○  | Keyboard and touch arrive with the app; a hidden tab should pause the race.                                                                                                                                                                                                                              |
| 38        | Failure and the crash report     |  ○  | The engine's log buffer is wired (`output-bridge.ts`); no report surface yet.                                                                                                                                                                                                                            |
| 39        | Text and localization            |  ○  | English only; player-facing strings owe one module.                                                                                                                                                                                                                                                      |
| 40.1      | Provenance                       |  ◐  | Physics constants will cite their source in `defs/tuning.ts`.                                                                                                                                                                                                                                            |
| 40.2      | Telemetry                        | ✅  | None, and the privacy page says so.                                                                                                                                                                                                                                                                      |
| 40.3      | A claim is not a fact            |  —  | No claims ship.                                                                                                                                                                                                                                                                                          |
| 41        | Game bootstrap checklist         |  ◐  | Infrastructure done; the game itself is being built.                                                                                                                                                                                                                                                     |

## What to do next, in the order it pays

1. **§25 — a determinism test and a sim digest.** The first thing to hold once the engine steps.
2. **§11.1 — the docs each subject owes** (architecture, getting started, riding, the generator, simulation), written as each lands.
3. **§23.9 / §35.1 — a startup budget and a reference device.** Cheaper to state before the renderer grows than after.
4. **§29.2 — prune the carried-over stylesheet** down to the rules the app draws and rename its variables to the snow palette.
