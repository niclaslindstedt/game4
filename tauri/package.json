{
  "name": "powderrun-tauri",
  "version": "0.1.0",
  "author": "Niclas Lindstedt <niclas@agilator.se>",
  "description": "Desktop wrapper around the Powder Run PWA — bundles the game on-device and serves it from a private scheme, in the platform's own webview. See README.md.",
  "private": true,
  "scripts": {
    "icons": "node scripts/icons.mjs",
    "icons:check": "node scripts/icons.mjs --check",
    "bundle": "node scripts/bundle-web.mjs",
    "bundle:copy": "node scripts/bundle-web.mjs --skip-build",
    "build": "npm run icons && cargo build",
    "start": "npm run bundle && npm run build && cargo run --bin powderrun --",
    "test": "cargo test -p powderrun-shell",
    "lint": "npm run icons && cargo clippy --workspace --all-targets -- -D warnings",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all -- --check",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
