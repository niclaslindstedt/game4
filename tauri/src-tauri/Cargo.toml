# SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
#
# THE EFFECTS. Everything in this crate opens, draws or talks to something; the
# decisions behind all of it live in `powderrun-shell`, which knows nothing
# about Tauri. See ../Cargo.toml.

[package]
name = "powderrun-tauri"
description = "The Tauri desktop shell around the Powder Run website — one window and the bundled game."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# `powderrun` rather than the package name, so the executable, the user-data
# folder and the docs all say the same word.
[[bin]]
name = "powderrun"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
powderrun-shell = { path = "../shell" }
serde_json = { workspace = true }
tauri = { version = "2", features = [] }
# The fatal-error box, and the one way a link out of the game (the repository,
# a credit) reaches the player's own browser instead of replacing the game.
# Both are called from RUST only — they are deliberately absent from
# `capabilities/default.json`, so the page cannot reach either.
tauri-plugin-dialog = "2"
tauri-plugin-opener = "2"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
