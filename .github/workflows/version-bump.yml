# SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
name: version-bump

# THE ONE WAY A RELEASE STARTS (OSS_GAME_SPEC §10.3): a maintainer dispatches
# this, and nothing else in the repository cuts a version.
#
# The spec's reference `version-bump` is a small, read-only workflow that
# resolves the next version, checks the tree, pushes a `v*` tag with a
# RELEASE_TOKEN PAT, and lets a `push: tags` trigger start the release. This
# repository keeps the small, read-only half — the checks below run before
# anything is written — and replaces the tag push with a `workflow_call` into
# release.yml, so the whole release is ONE dispatched run: the commit, the
# tag, the GitHub Release and the Pages deploy, all under the default
# GITHUB_TOKEN. No PAT is stored, no tag is force-pushed, and a hand-pushed
# `v*` tag starts nothing (tag protection still belongs on the repository,
# as the spec says — but the blast radius of a stray tag here is a tag).
#
# `bump` defaults to `auto` — derived from the .changes/unreleased/ fragments
# by scripts/release/compute-bump.mjs. Pick patch/minor/major to override.

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type (auto | patch | minor | major). 'auto' reads the changeset fragments."
        required: false
        default: auto
        type: choice
        options:
          - auto
          - patch
          - minor
          - major

permissions: {}

jobs:
  # The read-only preflight: the branch, the tree, the fragments, and the
  # version the release will produce — printed, so a maintainer sees what a
  # dispatch is about to do before release.yml writes anything.
  preflight:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      bump: ${{ steps.plan.outputs.bump }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci

      - name: Verify main, clean
        run: |
          if [ "$GITHUB_REF" != "refs/heads/main" ]; then
            echo "::error::releases are cut from main only (got $GITHUB_REF)"
            exit 1
          fi
          if ! git diff --quiet || ! git diff --quiet --cached; then
            echo "::error::working tree is not clean — refusing to release"
            git status
            exit 1
          fi

      - name: Plan the version
        id: plan
        env:
          REQUESTED: ${{ inputs.bump }}
        run: |
          if [ "$REQUESTED" = "auto" ] || [ -z "$REQUESTED" ]; then
            BUMP=$(node scripts/release/compute-bump.mjs)
          else
            BUMP="$REQUESTED"
          fi
          LATEST_TAG=$(git tag --list 'v*' --sort=-v:refname | head -n1)
          CURRENT="${LATEST_TAG#v}"
          CURRENT="${CURRENT:-0.0.0}"
          IFS=. read -r MAJOR MINOR PATCH <<<"$CURRENT"
          case "$BUMP" in
            major) MAJOR=$((MAJOR+1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR+1)); PATCH=0 ;;
            patch) PATCH=$((PATCH+1)) ;;
            *) echo "::error::unknown bump '$BUMP'"; exit 1 ;;
          esac
          NEXT="v${MAJOR}.${MINOR}.${PATCH}"
          if git rev-parse -q --verify "refs/tags/$NEXT" >/dev/null; then
            echo "::error::$NEXT already exists"
            exit 1
          fi
          echo "bump=$BUMP" >> "$GITHUB_OUTPUT"
          echo "Will release $NEXT (bump: $BUMP, from ${LATEST_TAG:-nothing})"

  release:
    needs: preflight
    uses: ./.github/workflows/release.yml
    with:
      bump: ${{ needs.preflight.outputs.bump }}
    permissions:
      contents: write
      pages: write
      id-token: write
    secrets: inherit
