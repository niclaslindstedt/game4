---
name: campaign
description: "Use when working on THE CAMPAIGN — the pinned maps and the ladder they make: adding, moving or re-naming a map in `pwa/src/game/campaign-levels.ts`, changing what a finish pays or what a lock asks for (`campaign.ts`), the campaign card and the level card (`menu-campaign.tsx`, `menu-levels.tsx`), the plate at the end of a rung (`campaign-run.ts`), which map a measured run is on (`pinnedFor`), the loops drawn on the boxes (`make routes`), or a generator change that has moved a pinned map (the version contract in `engine/mapgen/versions.ts`). Owns the curation loop — `make rate CAMPAIGN=1`, `make difficulty CAMPAIGN=1`, the sim on the map — and the rule that a pinned map's digest is rewritten only for a map deliberately moved. Not whether a map is any GOOD in the first place (`level-rating`), and not the cards' chrome (`menu-system`)."
---

# The campaign: the pinned maps, ridden for points

The campaign is the sibling games' championship retyped for snow: three
SHELVES (the foothills, the timberline, the summit), six MAPS each — a race,
a time trial, two races, a time trial and a race, so the race both opens and
closes a shelf — every map a SEED on a generator VERSION, ridden on the day
its seed dealt (or a `sky` laid over it). A race rung is ridden against the
race's three rivals with contact off and pays the podium three, two and one
for the whole field; a trial rung is ridden alone and pays a MEDAL against
times set off the bot's own run. The next map opens behind a podium or a
medal, the next shelf behind the table. `campaign.ts`'s header says why each
of those is the shape it is.

The same eighteen maps are the game's MEASURED maps: a RACE and a TIME TRIAL
off the front door pick one on the level card (`menu-levels.tsx`) out of the
shelves the campaign has opened, so two times in the record book are two
times round the same loop. A seed of one's own is the free ride's, and a
link's `?seed=` (`pinnedFor`).

**Read this skill's lessons first** — `node scripts/skill-lessons.mjs
campaign --list`. Load **`skill-reflection`** at both ends, **`write-code`**
beside this one for any code change, **`level-rating`** whenever a map is
being chosen or judged, **`mapgen-improvement`** when a generator change is
what moved a map, and **`menu-system`** for anything about the cards
themselves.

## Where everything lives

| Thing | File |
| --- | --- |
| The maps: seed, laps, version, digest, the day on the box, medals | `pwa/src/game/campaign-levels.ts` |
| The policy: building a map, standing a run up, `pinnedFor`, the points, the locks, the table, the stored board | `pwa/src/game/campaign.ts` (storage-free above the line) |
| A rung booked at the flag, and the plate's lines | `pwa/src/game/campaign-run.ts` (the rig, `campaignPlateFor`) |
| A pinned run stood up, and stood up again | `pwa/src/game/pinned-run.ts` (a factory over `App.tsx`'s loader) |
| The campaign card: the shelf tabs, the six boxes, the table | `pwa/src/game/menu-campaign.tsx` |
| The level card: the map a RACE or a TIME TRIAL rides | `pwa/src/game/menu-levels.tsx`, both routed by `menu-pinned.tsx` |
| The loop behind each box | `pwa/src/game/route-shape.ts` (the format) + `campaign-routes.ts` (GENERATED by `make routes`) |
| Its words | `pwa/src/game/strings-campaign.ts`, spread into `STRINGS` |
| Its styles | `pwa/src/campaign.css` |
| The generator's versions and the digest | `engine/mapgen/versions.ts`, `digest.ts` |
| Tests | `tests/campaign_test.ts` (the policy, the plate, the rig), `tests/generator_version_test.ts` (every map rebuilt and held to its digest, its day and its route) |

## The curation loop — adding or moving a map

```
   1. make rate COUNT=96 ARGS=--sim                    the pool, the bot's lap as the time axis
   2. shortlist on the AXES, not the index            level-rating's brief
   3. make difficulty SEED=<seed> ARGS=--sim          LOOK at it
   4. edit campaign-levels.ts: seed, mode, laps, name, blurb, medals
   5. make rate CAMPAIGN=1           every rung asks more? no pair the same map twice?
      — it prints the DIGEST each map builds to and the bot's time over the
        rung's laps; write the digest and the day (`day`) into the row
   6. make routes                     the loop behind the box
   7. make difficulty CAMPAIGN=1      the sheets, in the PR
   8. npx vitest run tests/generator_version_test.ts tests/campaign_test.ts
```

**The day is the cheapest lever.** Every rung rides the day its seed dealt
(R15, R19) unless its row lays a `sky` over it; a weather and a start hour
cost nothing that has to be re-verified and carry a fifth of the index
(`RATING.weight`), so when two rungs sit level, try the day before the seed:
a seed change re-rolls the map, the sheet, the bot's time, the route and the
name. The box bills the day it is ridden in (`day`), and the version test
holds that to the built map, so a `sky` added to a row owes its `day` too.

**The medals are set against the bot.** `make rate CAMPAIGN=1` rides every
rung with the bot over its own laps and prints the time beside the medals:
silver is the bot's time and a few per cent, bronze an eighth slower, gold
two per cent under it. A medal is the DOOR to the next rung, so a bronze the
bot could not reach is a shut door — the audit says so with `!!`.

**The ladder is read on the MEASURED lap.** The rating's time axis prefers
the bot's lap to the estimate off the loop's length, and the bot's pace
varies a lot from map to map — so a ladder that climbs under `make rate
CAMPAIGN=1` can step down under a bare `rateLevel`. The audit is the
measurement; the test suite does not ride the bot on every rung.

**A map is named for what it is like, never for where it is** — the snow,
the light, the shape of the ask. There is one nature and no places in it.

## The version contract, and the digest

Every map names the generator version it was curated under and carries the
digest of the map that came out. `tests/generator_version_test.ts` rebuilds
every one and compares. When it goes red there are exactly two cases, with
opposite fixes:

- **You moved the map** (a new seed, a deliberate move onto a newer
  version): run steps 5 and 6 above and write the new digest down. The
  picture was meant to change.
- **The rules moved under a map that did not**: NEVER regenerate the digest.
  Add a row to `GENERATOR_VERSIONS`, keep the old behaviour on the old row
  as an optional trait read through `generatorTraits(opts.version)` at the
  one place it differs, and leave the map where it stands — or move it onto
  the new version as a curation, one map at a time. Bumping every map
  because the suite went red is the implicit re-roll the scheme exists to
  prevent, and it puts every player's board and record book on water nobody
  rode.

And **delete a version nothing names any more**, row and trait branches
together — the test refuses a museum.

## What a change here obliges

- `make rate CAMPAIGN=1` and `make difficulty CAMPAIGN=1` in the PR, and
  `make routes` for any map that moved.
- `docs/getting-started.md`'s campaign paragraph; a renamed shelf is
  renamed there.
- A new word on the cards goes in `strings-campaign.ts`; a new mark in
  `menu-glyphs.tsx`.
- A changed board shape (`CampaignProgress`) needs a case in
  `tests/campaign_test.ts` for what an older blob does to it — the board is
  a player's save — and a re-cut shelf (ids are rung numbers) a new
  `PROGRESS_KEY`.
- Anything the player sees → a `.changes/unreleased/` fragment.

## Skill self-improvement

Load **`skill-reflection`** before this session commits. Worth a fragment
here: a rung that read one way on the index and another in the game, a lock
a player found a way round, a re-roll the digest caught.
